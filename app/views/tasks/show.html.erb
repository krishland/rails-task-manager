<h1><%= @task.title %></h1>
<%= link_to "Edit task", task_edit_path %>
<p><%= @task.details %></p>
<p><%= @task.completed ? "This task is completed" : "This task is not completed yet" %></p>
<%= link_to "Back to list", tasks_path %>
